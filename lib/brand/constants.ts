/** Ruta pública del icono de marca (servido desde /public). */
export const CONVERSA_ICON_SRC = '/IconConversa.png';

/** Dominio público de producción. */
export const CONVERSA_DOMAIN = 'conversa-io.chat';

/** Nombre visible de la app / sitio. */
export const CONVERSA_APP_NAME = 'conversa-io.chat';

/** URL canónica HTTPS del sitio. */
export const CONVERSA_SITE_URL = `https://${CONVERSA_DOMAIN}`;
